#include <stdio.h>
#include "main.h"
/**
 * print_rev - function that prints the string in reverse
 * @s: variable
 * Return: 0
 */
void print_rev(char *s)
{
  int n = 0;
while (s[n] != '\0')
n++;
}
j = n - 1;
for (i = 0; i < count; i++)
{
rev[i] = str[j];
j--;
}

 
