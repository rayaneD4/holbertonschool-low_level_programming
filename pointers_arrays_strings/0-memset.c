#include <stdio.h>
#include "main.h"

/**
 * _memset - Entry point
 * @s: pointer
 * @b: character
 * @n: integer
 *
 * Return: Always s (Success)
 */
char *_memset(char *s, char b, unsigned int n)
{
char *m;
size_t i;

c = m;
i = 0;
while (i < n)
{
m[i] = c;
i++;
}
return (s);
}
