#include <stdio.h>
#include <string.h>
#include "3-calc.h"
/**
 * get_op_func - the function that selects the correct function
 * @s: function
 * Return: 0
 */
int (*get_op_func(char *s))(int, int);
{
